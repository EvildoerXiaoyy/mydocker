# 部署配置文件模板
# 复制此文件为 config.env 并修改相应的配置值

# 目标服务器配置
TARGET_SERVER="your-server-ip"
TARGET_USER="your-username"
TARGET_PATH="/path/to/deploy"

# 项目配置
PROJECT_NAME="test-go"

# 可选：SSH 端口（默认 22）
# SSH_PORT="22"

# 可选：SSH 密钥文件路径
# SSH_KEY_PATH="$HOME/.ssh/id_rsa"

# 可选：SSH 连接超时（秒）
# SSH_TIMEOUT="10"

# 配置说明：
# 1. TARGET_SERVER: 目标服务器的 IP 地址或域名
# 2. TARGET_USER: SSH 登录用户名
# 3. TARGET_PATH: 服务器上存放部署文件的目录路径
# 4. PROJECT_NAME: 项目名称，用于标识
# 5. SSH_PORT: SSH 服务端口，如果不是默认的 22
# 6. SSH_KEY_PATH: 私钥文件路径，如果不在默认位置
# 7. SSH_TIMEOUT: SSH 连接超时时间，单位为秒

# 使用示例：
# TARGET_SERVER="192.168.1.100"
# TARGET_USER="root"
# TARGET_PATH="/opt/myapp"
# PROJECT_NAME="my-docker-project"
